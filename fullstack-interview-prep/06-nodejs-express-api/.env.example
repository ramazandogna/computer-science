# Copy to `.env` for local runs. docker-compose injects these for the container.
PORT=3000
NODE_ENV=development

# Redis connection. In docker-compose the host is the service name "redis";
# running bare on your machine it's "localhost".
REDIS_URL=redis://localhost:6379

# Cache TTL (seconds) for cached reads.
CACHE_TTL_SECONDS=60
